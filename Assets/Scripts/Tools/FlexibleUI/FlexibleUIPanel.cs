using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FlexibleUIPanel : FlexibleUI
{
    protected override void SkinIcon()
    {
        //We don't want to skin icon that's why we override with an empty method
    }
    protected override void SkinText()
    {
        //We don't want to skin text that's why we override with an empty method
    }
}
